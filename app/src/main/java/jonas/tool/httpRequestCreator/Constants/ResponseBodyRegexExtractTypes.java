package jonas.tool.httpRequestCreator.Constants;

public class ResponseBodyRegexExtractTypes {
	public static final String[] TYPES = {
		"All matches only",
		"All matches, with full line (grep style)",
		"First match only",
		"First match, full line",
		"Last match only",
		"Last match, full line",
	};
}
